<!DOCTYPE html>
<html lang="en">

<head>
    <title>Agregar Doctores</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            background-color: #361E1E;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            margin: 0;
            color: white;
            text-align: center;
        }

        .container {
            width: 100%;
            max-width: 400px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label,
        input,
        select {
            margin: 10px 0;
            width: 100%;
        }

        input[type="submit"] {
            width: auto;
            padding: 10px 20px;
        }
    </style>
    <script>
        function submitForm(event) {
            event.preventDefault();
            var xhr = new XMLHttpRequest();
            xhr.open("POST", '/SM/doctores', true);
            xhr.setRequestHeader('Content-Type', 'application/json');
            xhr.send(JSON.stringify({
                nombre_doctor: document.getElementById('nombre_doctor').value,
                apellido_doctor: document.getElementById('apellido_doctor').value,
                Especialidad: document.getElementById('Especialidad').value
            }));
            xhr.onload = function () {
                if (xhr.status == 200) {
                    alert("Se agregó un nuevo doctor");
                } else {
                    alert("error al agregarse");
                }
            };
        }
    </script>
</head>

<body>
    <h1>Agregar Doctores</h1>
    <form onsubmit="submitForm(event)">
        <label for="nombre_doctor">Nombre:</label><br>
        <input type="text" id="nombre_doctor" name="nombre_doctor"><br>
        <label for="apellido_doctor">Apellido:</label><br>
        <input type="text" id="apellido_doctor" name="apellido_doctor"><br>
        <label for="Especialidad">Especialidad:</label><br>
        <select id="Especialidad" name="Especialidad">
            <option value=""></option>
            <option value="Cardiologo">Cardiologo</option>
            <option value="Pediatra">Pediatra</option>
            <option value="Dermatologo">Dermatologo</option>
            <option value="Podologo">Podologo</option>
            <option value="Ortopedia">Ortopedia</option>
        </select><br>
        <input type="submit" value="Agregar Doctor">
    </form>

</body>

<footer>
    <button onclick="window.location.href='/';">
        Volver
    </button>
</footer>

</html>